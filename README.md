# temperature_probe

this project investigates whether a probe trained on residual stream activations can be used to determine the temperature a model is being sampled from, how that varies by layer, how that varies by scale, how that varies during generation, and whether this can be steered.